import React from 'react';

const Pagetitle = ()=>{
           return(
            <div>
                <h1 className="title-container__title">Check the Weather Conditions !!</h1>
                <h4 className="title-container__subtitle">You can check temperature,
                            conditions and some more..............</h4>
            </div>
        );
}

export default Pagetitle;